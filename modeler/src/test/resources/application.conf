config {

  // Debug flag
  debug = true

  // karma stuff
  karma-dir = /tmp/test-ssd/karma

  // if jenkins is running we launch inside the jenkins environment
  karma-dir = ${?JENKINS_HOME}/tmp/test-ssd/karma
}